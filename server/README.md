below are the instructions on running this server

...in terminal
npm install

... in package.json
    inside the script object just after the "test"
      add ...

        "dev": "nodemon index.js"


...in terminal
npm run dev
